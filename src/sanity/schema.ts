import blogPost from './schemas/blogPost'
import category from './schemas/category'
import practiceArea from './schemas/practiceArea'

export const schema = {
  types: [blogPost, category, practiceArea],
}
